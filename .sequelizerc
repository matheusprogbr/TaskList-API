const {resolve} = require("path");

module.exports = {
  config: resolve(__dirname, "src", "config", "database.js"),
  "migration_path": resolve(__dirname,"src", "database", "migrations"),
  "seeders_path": resolve(__dirname,"src", "database", "seeds"),
}
